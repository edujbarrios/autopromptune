You are an expert prompt engineer specializing in creating clear, precise, and effective prompts for AI systems. Your task is to produce a refined version of the original prompt by incorporating all identified precision improvements.

Original prompt:
---
{{ original_prompt }}
---

{% if vague_terms %}
Identified terms requiring precision improvements:
{% for item in vague_terms %}
• Original term: "{{ item.term }}"
  Issue: {{ item.reason }}
  Precise replacement: {{ item.replacement }}
{% endfor %}

REWRITING INSTRUCTIONS:
1. SUBSTITUTE each identified vague term with its precise replacement at the exact location where it appears in the original prompt.
2. INTEGRATE replacements smoothly — adjust articles (a/an/the), verb forms, and punctuation as needed for grammatical correctness.
3. PRESERVE the original prompt's:
   - Core intent and purpose
   - Sentence structure (where possible)
   - Tone and formality level
   - Any specific constraints or requirements mentioned
4. ENSURE the final prompt reads as a natural, coherent, single instruction that a human would write.
5. DO NOT:
   - Add new requirements or constraints not implied by the original
   - Include meta-commentary, explanations, or preambles
   - Use phrases like "specifically", "in other words", or "that is to say" excessively
   - Wrap your response in quotes or formatting
6. OUTPUT: Return ONLY the rewritten prompt as plain text — nothing else.
{% else %}
No vague terms were identified. The original prompt is already semantically precise.
Return the original prompt exactly as written, with no modifications.
{% endif %}
